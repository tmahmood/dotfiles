[opener]
lnav_cli = [
    { run = 'lnav "$@"', for="unix", desc="lnav in shell", block=true }
]
jless = [
    { run = 'jless "$@"', for="unix", desc="jless", block=true }
]
vd = [
    { run = 'vd "$@"', for="unix", desc="visidata", block=true }
]
[open]
prepend_rules = [
	{ mime = "*.log", use = "lnav_cli", block = true, desc = "lnav cli" },
	{ mime = "*.{json,jsonl,jsonStream}", use = "jless", block = true, desc = "jless" },
	{ mime = "*.{sqlite,csv}", use = "vd", block = true, desc = "visidata cli" },
]

